# PumpGuard DT -- backend runtime and test dependencies.
#
# Deliberately unpinned to major-version floors: this is a portfolio project
# that must install cleanly on a fresh Python 3.14 today, not reproduce a
# frozen 2024 environment. Pin exactly if this is ever deployed.

# --- numerics / signal processing ---------------------------------------
numpy>=2.1
scipy>=1.14
pandas>=2.2

# --- machine learning ----------------------------------------------------
scikit-learn>=1.5
joblib>=1.4

# --- API layer -----------------------------------------------------------
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.9
websockets>=13.0

# --- test ----------------------------------------------------------------
pytest>=8.3
httpx>=0.27
